from alerts.alert_manager import AlertManager

manager = AlertManager()

risk_scores = {
    1: 0.12,
    2: 0.41,
    3: 0.63,
    4: 0.91,
}

levels = manager.update(risk_scores)

for track_id, level in levels.items():
    print(track_id, level)